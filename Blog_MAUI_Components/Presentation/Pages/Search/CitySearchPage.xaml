<?xml version="1.0" encoding="utf-8"?>
<base:ContentPageBase
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:base="clr-namespace:Blog_MAUI_Components.Presentation.Common"
    xmlns:page="clr-namespace:Blog_MAUI_Components.Presentation.Pages.Search"
    xmlns:model="clr-namespace:Blog_MAUI_Components.Application.Cities;assembly=Blog_MAUI_Components.Application"
    Title="Search Cities"
    x:DataType="page:CitySearchPageViewModel"
    x:Class="Blog_MAUI_Components.Presentation.Pages.Search.CitySearchPage">

    <Grid RowDefinitions="*,Auto" >
        <CollectionView ItemsSource="{Binding Cities}"
                        SelectionMode="Single"
                        SelectedItem="{Binding SelectedCity}"
                        SelectionChangedCommand="{Binding CitySelectedCommand}" >
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:CityVm">
                    <StackLayout Padding="10" Orientation="Horizontal">
                        <Label Text="{Binding Name}"
                               FontSize="Medium"
                               VerticalOptions="Center" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
    
</base:ContentPageBase>
